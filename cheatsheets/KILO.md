# KILO

## shortcuts

- `leader + e` opens $EDITOR for long form writing
- `leader + m` select a model
- `leader + t` select theme
- `leader + t` select theme
- `ctrl + alt + u` half a page up
- `ctrl + alt + d` half a page up
- `leader + n` new session
- `leader + l` switch sessions

