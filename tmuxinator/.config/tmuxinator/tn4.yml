# ~/.config/tmuxinator/tn4.yml

name: tn4
root: ~/dev/tn4

windows:
  - editor:
      panes:
        -  while true; do nvim; done
  - shell:
  - git:
      panes:
        -  while true; do lazygit; done
  - console:
      panes:
        -  while true; do bin/rails console; done
  - server:
      panes:
        -  while true; do bin/rails server; done
